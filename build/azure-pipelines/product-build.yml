# Retired: Norris Writer uses GitHub Actions (.github/workflows/norris-writer.yml)
# This pipeline is intentionally disabled.

pr: none

trigger: none

schedules: []

jobs:
  noop:
    displayName: Retired
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: echo "Azure Pipelines retired. Use GitHub Actions instead."
        displayName: Notice
