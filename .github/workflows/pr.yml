# Retired: replaced by norris-writer.yml
# This workflow is intentionally disabled.

name: Code OSS (retired)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/norris-writer.yml instead."
